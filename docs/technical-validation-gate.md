# CompatRadar technical-validation gate

Generated by `scripts/technical-validation-gate.ps1` with application and CLI telemetry disabled.

## Gate status

- Status: ENVIRONMENT_LIMITED (exit code 2).
- Repository revision: `eb3676894fb9bf883e4d8df902744dce9f8aa517`.
- Environment-limited records: preview-sdk: not-available - Preview SDK '11.0.100-preview.7.26381.103' is not installed or visible to the current dotnet host. real-repository-corpus: not-run - Real repository paths were available, but the requested preview SDK/runtime was not available.

## Deterministic corpus

- Planted deterministic future-breakage scenarios: 6/6 detected (100%, exceeding the >=95% gate).
- Equivalent stable/future state false `FUTURE_REGRESSION` results: 0.
- Additive-feed integration: PASS (`CandidateFeedRestoresWatchedPackageWhileNormalSourceRemainsAvailable`) in the Release test suite and named gate check.
- Stable-control failures: classified `INCONCLUSIVE_BASELINE_FAILED`, including non-deterministic fail-A/fail-B output.
- Candidate fail-A/fail-B output: classified `INCONCLUSIVE_FLAKY`, never `FUTURE_REGRESSION`.
- Flaky and non-monotonic sequences: covered by the named regression tests; no unjustified first-bad claim is emitted.
- Reachable unsupported path: missing SDK/runtime candidate is classified `UNSUPPORTED` with exit code 2.

## Named gate checks (10)

- StableAndIdenticalCandidateAreCompatibleAndDoNotMutateWorktree: PASS — COMPATIBLE; active worktree unchanged.
- ReproducibleCandidateFailureIsFutureRegression: PASS — FUTURE_REGRESSION.
- DifferentCandidateFailuresAreInconclusiveRatherThanFutureRegression: PASS — INCONCLUSIVE_FLAKY.
- DifferentStableFailuresRemainBaselineInconclusive: PASS — INCONCLUSIVE_BASELINE_FAILED.
- DifferentExitCodesWithoutDiagnosticsAreInconclusive: PASS — INCONCLUSIVE_FLAKY.
- MonotonicCandidateSequenceLocalizesFirstConfirmedFailure: PASS — first confirmed bad candidate localized.
- NonMonotonicSequenceDoesNotClaimFirstBad: PASS — observed failing candidates; no first-bad claim.
- FlakyCandidateIsInconclusive: PASS — INCONCLUSIVE_FLAKY.
- MissingSdkCandidateIsUnsupported: PASS — UNSUPPORTED.
- CandidateFeedRestoresWatchedPackageWhileNormalSourceRemainsAvailable: PASS — additive feed available alongside normal source.

## Runtime, restore, and corpus evidence

- SDK: 8.0.425.
- Runtime/build/test elapsed: restore 1.01 seconds; build 3.1 seconds; test corpus 112.92 seconds; additive-feed test 2.35 seconds.
- Runtime-preview candidate: Not available: 11.0.100-preview.7.26381.103 was not visible to the current dotnet host.
- Real repository probes: Not run: the preview candidate prerequisite was not available.
- Inconclusive rate: 4 of 10 named gate checks (40%) intentionally exercise baseline/flaky ambiguity; the separate unsupported check is also non-success. Every result remains visible in the JSON report.
- Runtime and restore cost: measured above for the gate host and per real-repository probe.

## Environment-limited host notes

- A prior verbatim run on the Windows review host stopped during the Release build with child build-server `Access denied`/`CS2012` errors. This is a build-server prerequisite limitation, not a relaxed gate result; the same exact source build completed with shared build servers disabled.
- On that host, the full local suite's cancellation test also could not build its child fixture (`CrossPlatformTests.cs:151`, child build exit 1). This remains environment-limited and is not counted as a local pass.
- Exact-fix-ref public CI counter-evidence: run [34540539354](https://github.com/KeelMatrix/CompatRadar/actions/runs/34540539354) passed all 9 jobs at `658aeb1a87903fc44d29aff680d11d33075a1a04`, including the Windows/Linux/macOS matrix, technical-validation gate, package inspection, Action smoke, and all three package-consumer smoke jobs. Final-main CI is recorded in the engineering handoff.

## Report assessment

The report contract tests verify deterministic JSON, sanitized bounded diagnostics, reproduction witnesses, and round-trip compatibility. An independent reviewer must assess usefulness of the generated witness and diagnostic attribution against this public gate artifact; this script does not self-approve that review.

## Reproduction

```powershell
$env:KEELMATRIX_NO_TELEMETRY = '1'
pwsh -NoProfile -File scripts/technical-validation-gate.ps1 -OutputPath artifacts/technical-validation-gate.md
```
